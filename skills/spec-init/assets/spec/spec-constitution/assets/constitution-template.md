# [PROJECT_NAME] Constitution

## Glossary

<!-- Example (with terms):
| Term   | Definition                                              |
| ------ | ------------------------------------------------------- |
| User   | An authenticated human interacting with the system via the UI. |
| Actor  | Any entity (human or service) that triggers a system action. |
-->

<!-- Example (no domain-specific terms):
| Term | Definition |
| ---- | ---------- |
| N/A  |            |
-->

## Principles

<!-- Example (Testing principle):
### I. Test-First Development

Rationale: Tests are the primary enforcement mechanism for safe, verifiable change.

#### Test Quality

- Tests MUST be deterministic: same input produces same result every run.
- Tests MUST be isolated: no shared state, order-independent, parallel-safe.
- Tests MUST follow AAA pattern (Arrange, Act, Assert).

#### Test Coverage

- New features MUST have integration and unit tests covering all acceptance scenarios from the spec.
- Bug fixes MUST include a regression test that fails before the fix and passes after.
- Critical paths MUST have 95% line coverage; common paths SHOULD have 80% line coverage.
-->

<!--
Example (Security/Compliance principle):
### II. Data Privacy

Rationale: User data must be protected at rest and in transit to meet regulatory requirements.

#### Data Protection

- User PII MUST be encrypted at rest using AES-256 or equivalent.
- All data transmission MUST use TLS 1.2 or higher.
- Users MUST be able to request deletion of their personal data within 30 days.
-->
